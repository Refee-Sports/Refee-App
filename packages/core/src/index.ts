// Shared by apps/web and apps/mobile. Regenerate the types after every
// migration with `npm run db:types` from the repo root.
export type { Database, Json, Tables, TablesInsert, TablesUpdate, Enums } from "./database.types";
